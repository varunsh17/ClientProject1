HOME 
    ABOUT
    PRODUCTS
    CONTACT

# Each Page has separate CSS file linked. 
# Main.js is the js file for every script/function call.
# SMTP.JS is used for contact us page to mail.
# For product us page, filter is done using Javascript whose function is written in Main.js
# Bootstrap used is @5 and sometimes @4
